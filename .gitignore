# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# Video files - hosted on Supabase Storage
public/videos/*.mp4

# Supabase temp files
supabase/.temp/

# Lavish artifact working dir (local review surfaces)
.lavish/
.playwright-mcp/

# Local preview renders and session scratch (never commit)
.preview/

# Local working files that live in the repo dir but never belong in git.
# (A gate run requires a clean tree; these are session scratch, owner notes,
# and local tooling config - commit any of them deliberately if that changes.)
# Nothing named claude.md belongs here: macOS checkouts set core.ignorecase, so
# a lowercase pattern also hides the conventional CLAUDE.md, and no negation can
# separate the two on this platform. The project instructions are tracked as
# CLAUDE.md instead.
/legal/
/*.png
/*.jpeg
/*.jpg
/.githooks/
/.gitleaks.toml
/supabase/config.toml
/supabase/.gitignore
/scripts/crew-dispatch-live-test.ts
/scripts/crew-dispatch-preview.ts
/scripts/newsletter-samples.ts
/scripts/seo-live-check.ts

# perf harness transient outputs (baseline.json IS committed)
/perf/.tmp/
