// This component will be migrated later when needed
// SEO schemas are handled through Next.js metadata API
// Keeping as placeholder for compatibility

interface LocalSEOSchemaProps {
  location: any;
  service?: string;
}

const LocalSEOSchema: React.FC<LocalSEOSchemaProps> = ({ location, service }) => {
  return null;
};

export default LocalSEOSchema;
